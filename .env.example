# Google Sheets
GOOGLE_SHEETS_ID=
# Either paste full service account JSON inline (CI usage)...
GOOGLE_SHEETS_CREDENTIALS_JSON=
# ...or point to a local JSON file (preferred for local dev)
GOOGLE_APPLICATION_CREDENTIALS=

# Logging
LOG_LEVEL=INFO

# Crawl window (days)
CRAWL_WINDOW_DAYS=7
