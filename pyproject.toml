[project]
name = "hienhd-market-watch"
version = "0.1.0"
description = "Auto crawl + AI-assisted Market Watch weekly report"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
